#include "Objects/Mouse.h"


void Mouse::move(sf::Vector2f boardSize)
{
	/*
	m_prev_pos = m_sprite.getPosition();
	
	//int randomDirection = (rand() % 2);
	float distance = 120.9f;

	


	//if ((passedTime > switchDirection || wallCollision) &&

	
		if(isCentered(m_sprite.getPosition(), getCenter(m_sprite.getPosition())
		{
		//wallCollision = false;
		//m_moving = true;
			
		}
	

	m_current_direction = m_arr[rand() % 2];
	sf::Sprite sprite;
	switch (m_current_direction)
	{
	case RIGHT:
		sf:: Sprite sprite->move(distance, 0);
		break;
	case LEFT:
		m_sprite.move(-distance, 0);
		break;
	}

	if (outOfBoard(boardSize))
		m_sprite.setPosition(m_prevPos);
	m_pos = m_sprite.getPosition();
	
	*/


}